server:
  port: 8081

spring:
  application:
    name: "assignment-submission-app"
  datasource:
    url: "jdbc:mysql://localhost:3306/assignment_submission_db"
    username: "root"
    password: "password123"
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
        driverClassName: com.mysql.cj.jdbc.Driver
